/ 戦闘力表示画面
    表示：
    戦闘力
    キャラ名
    テキスト
    twitterへのシェアボタン
      シェアボタン押下で戦闘力、キャラ名、テキスト部分のスクリーンショットを取り、
      twitterへ投稿

/ twitterシェアボタン：
  ・ボタン押下時にキャプチャ取得する
    →user#take_screenshotに移る
  ・「リンクをあなたのフォロワーに共有する」画面に遷移する
  ・取得したキャプチャをtwittercardで作成する際に、twitter:imageに指定します
= link_to 'アクション', share_twitter_path(url: @url, path: @screenshot_path, file_name: @screenshot_name)

/ TODO デザインは未反映
.power
  | あなたの戦闘力は
  = get_sum_power(@user, 'total')
  | です
.name
  | あなたは
  = @user.character_name
  | です
.text
  p
    = @user.character_introduction
p
script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.7.1/Chart.min.js"
canvas#lineChartCanvas
/ jsファイルに分けるほうが、変数の受け渡しや可視性の面が劣るため、htmlに記述
javascript:
  new Chart(document.getElementById("lineChartCanvas"), {
    "type": "line",
    "data": {
      "labels": #{get_days_from_today(30)},
      "datasets": [{
        "label": "戦闘力遷移",
        "data": #{@data_30days},
        "borderColor": "rgb(75, 192, 192)",
        "lineTension": 0.1
      }]
    },
    "options": {}
  });

